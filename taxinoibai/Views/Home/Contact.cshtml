@using taxinoibai.Models
@{
    var config = (ConfigSite)HttpContext.Current.Application["ConfigSite"];
    ViewBag.Title = "Liên hệ";
}
<section class="page-section ">
    <div class="container">
        <div class="row">
            <div class="col-lg-6">
                @Html.Action("Form")
            </div>
            <div class="col-lg-6">
                <div class="map overflow-hidden">@Html.Raw(config.GoogleMap)</div>
            </div>
        </div>
    </div>
</section>